# Build outputs
build/
.gradle/
local.properties
.idea/

# Model file (bundled in assets at build time, never committed)
*.tflite
app/src/main/assets/*.tflite

# Gradle wrapper binary / scripts (not committed)
gradlew
gradlew.bat
gradle/wrapper/gradle-wrapper.jar

# Python conversion env
scripts/__pycache__/
