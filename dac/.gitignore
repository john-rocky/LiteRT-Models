# Build outputs
build/
.gradle/
local.properties
.idea/

# Model files (the big tflites are pushed to filesDir, never committed)
*.tflite
app/src/main/assets/*.tflite

# Gradle wrapper binary / scripts (not committed)
gradlew
gradlew.bat
gradle/wrapper/gradle-wrapper.jar

# Python conversion env
scripts/output/
scripts/out/
scripts/__pycache__/

# Re-include the small required assets (root .gitignore excludes *.bin)
!app/src/main/assets/dac_rvq.bin
!app/src/main/assets/test_audio.bin
