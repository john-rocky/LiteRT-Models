# Build outputs
build/
.gradle/
local.properties
.idea/

# Model files (the tflites + the 69 MB mimi_rvq.bin are pushed to filesDir, never committed)
*.tflite
app/src/main/assets/*.tflite
mimi_rvq.bin

# Gradle wrapper binary / scripts (not committed)
gradlew
gradlew.bat
gradle/wrapper/gradle-wrapper.jar

# Python conversion env
scripts/output/
scripts/out/
scripts/__pycache__/

# Re-include the small required asset (root .gitignore excludes *.bin)
!app/src/main/assets/test_audio.bin
